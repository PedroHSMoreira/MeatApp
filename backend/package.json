{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "sucrase ./src -d ./dist --transforms typescript,imports &&  nodemon ./dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.15.1"
  },
  "dependencies": {
    "jsonwebtoken": "^8.5.1"
  }
}
